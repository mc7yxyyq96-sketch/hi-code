# PARITY Wave2 — Build/Plan + headless/ACP

## Shipped

- **E-01** Agent modes `build` / `plan` / `ask` with permission matrix (`src/agent-modes.ts`)
- Composer **Agent mode pill** + `/agent-mode` `/plan` `/ask`
- System prompt guidance per mode
- **E-02** grep/glob `limit` + glob `max_depth`
- **E-03** `hicode-headless` entry + ACP stub interface

## Tests

- `node test/agent-mode-tests.mjs`
- renderer architecture 162+
