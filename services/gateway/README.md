# Hi Code Gateway (Wave3)

Clean-room OpenClaw/Hermes-inspired local gateway.

Planned responsibilities:
- session routing
- channel adapters (Telegram + one more)
- MCP catalog control plane
- desktop remote connect (WS auth)
- upstream Model Gateway / NewAPI relay (no client master keys)

Status: scaffolding only on `cursor/parity-superset`.
